{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost; Database=library; Trusted_Connection=True; TrustServerCertificate=True; MultipleActiveResultSets=True;",

    // We can see about either trying to make the server accessible to all of us, So likely based online somewhere.
    // Or we can add other ConnectionStrings for Example:
    "EvaConnection": "",
    "SelinaConnection": ""

    // Although the problem with that is that it would devide up the Database, meaning we would all have our own seperate versions.
    // Which means any changes made to one, would not affect any of the others.
  }
}